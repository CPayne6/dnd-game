{
  "name": "core",
  "version": "0.0.0",
  "description": "The core character logic for 5e",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Payne",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
